class Solution:
    def __init__(self):

        self.word1 =  input("Enter the word1 : ")
        self.word2 = input("Enter the word2 : ")
        self.merged = []

    def merge(self):
        
        for i in range(len(self.word1)):
            self.merged = i.append()
            break
        for i in range(len(self.word2)):
            self.merged =  i.append()
            continue

merged_word = Solution()
print(merged_word)